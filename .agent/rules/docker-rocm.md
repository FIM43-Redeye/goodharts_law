---
trigger: always_on
---

This project uses Docker to run PyTorch on an AMD system (the main dev's system), but the Dev Containers extension is not available. You need to use Docker Compose commands (see docker_directions.txt) to run models on the GPU.